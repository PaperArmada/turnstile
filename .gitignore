# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/

# uv
uv.lock

# Process engine runtime state: in-flight instances and the text log
# are ephemeral; the completed/abandoned ledger is the audit record
# and IS committed so CI can verify it (docs/verification.md)
.process-state/active/
.process-state/log.txt

# Machine-specific (contains absolute paths, regenerate with `turnstile init`)
.mcp.json
.claude/

# Generated JSON schemas (regenerate with `turnstile schema`)
.processes/schemas/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
